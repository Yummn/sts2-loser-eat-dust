# 败者食尘 v0.3.1

## 修改

- 从旧节点回溯后，后续已有记录进入“新时间线覆盖”状态。
- 第一次重新经过某个已有记录节点时，用当前新时间线的房间入口状态替换旧记录。
- 每个节点在当前回溯时间线只允许覆盖一次，之后的战斗结束、事件完成及保存退出不会把入口记录覆盖成结束状态。
- 覆盖规则通过游戏存档后端持久保存，退出游戏后继续本局仍然生效。

## 验证

PC v0.107.1 实战存档后端测试：旧节点记录首次重新经过时成功替换；同节点第二次保存未再次覆盖新入口状态。

```text
[败者食尘] rewound timeline begins after visit 1
[败者食尘] overwrote r1_c3: act=1, visit=2
[CodexLoserEatDustOverwriteTest] PASS
```
